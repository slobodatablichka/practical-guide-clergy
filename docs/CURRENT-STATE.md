# CURRENT STATE

## Готово

- GitHub-репозиторий: `slobodatablichka/practical-guide-clergy`;
- локальная рабочая копия: `C:\Projects\Practical-Guide-Clergy`;
- 30 билетов, **180 экзаменационных позиций**, **171 нормализованная тема**;
- канонические EPUB обоих учебников находятся в `assets/book/`;
- **Уч.1**: протоиерей Геннадий Нефёдов, «Таинства и обряды Православной Церкви»;
- **Уч.2**: протоиерей Николай Сильченков, «Практическое руководство при совершении приходских треб»;
- индекс Уч.1: **556 навигационных позиций**;
- индекс Уч.2: **309 навигационных позиций**;
- для всех 171 тем сформированы независимые наборы ответов обоих учебников;
- для каждого ответа хранится оценка релевантности `rel_1…rel_5`;
- при `rel_1` отсутствие прямого материала фиксируется явно, без достраивания общими знаниями;
- методика оценок закреплена в `docs/TEXTBOOK-SOURCES.md`;
- GitHub Actions проверяет оба EPUB, оба индекса, 342 ответа, диапазон `rel` и корректность EPUB-ссылок;
- в шапке сайта показаны оба учебника и отдельные ссылки скачивания EPUB;
- в билетах общая кнопка «Показать ответы» удалена;
- у каждого вопроса доступны **Уч.1 · rel_N** и **Уч.2 · rel_N**;
- одновременно в билете открыт только один выбранный ответ; повторное нажатие закрывает его;
- «Подробнее в учебнике» открывает именно выбранный EPUB и возвращает к тому же вопросу и источнику;
- раздел «По учебнику» переключается между Уч.1 и Уч.2 и использует порядок соответствующей книги;
- поиск учитывает ответы обоих учебников и также открывает один выбранный источник;
- EPUB-ридер работает с обоими учебниками, сохраняет мобильную адаптацию и подсветку фрагментов источника;
- GitHub Pages публикует ветку `main`.

## Текущий статус качества

Первичная разметка `rel` и первичное сопоставление Уч.2 построены автоматически по структуре и тексту EPUB. Они являются **рабочей первичной оценкой**, а не окончательной содержательной аттестацией.

На этапе автоматической проверки отдельно исправлены особенности структуры Сильченкова: повторяющиеся рубрики «Обрядовый порядок», «Время и место», «Церковные правила» привязаны к конкретной требе; пустые рубричные страницы не принимаются за источник.

## Требует содержательной проверки

- последовательная ручная проверка `rel` Уч.1 и Уч.2;
- редактура кратких ответов, начиная с тем, где `rel` различается сильнее всего;
- проверка первичных сопоставлений Уч.2 для `rel_2` и `rel_3`;
- выборочная проверка подсветки фрагментов обоих EPUB;
- пользовательское тестирование новой двухисточниковой механики на десктопе и телефоне.

## Следующий этап

1. Проверить на опубликованном сайте несколько билетов: переключение Уч.1 ↔ Уч.2 и правило «один открытый ответ».
2. Проверить переход «Подробнее в учебнике» и возврат для обоих EPUB.
3. После подтверждения интерфейса начать содержательную ревизию оценок и ответов по порядку вопросов.
